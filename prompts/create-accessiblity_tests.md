# Create Playwright accessibility tests (TypeScript)

Copy everything inside the block below into your assistant chat, then replace the bracketed section.

```
Write Playwright tests focusing on accessibility.

Requirements:
- TypeScript
- Use getByRole / accessibility-first approach (if no data-testid)
- Validate:
  - Roles
  - Labels
  - Visibility
  - Keyboard navigation

Scenario:
[PAGE OR FEATURE]

Output:
- Accessibility-focused test cases
```
