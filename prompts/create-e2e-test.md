# Create E2E test (Playwright + POM + data-testid)

Copy everything inside the block below into your assistant chat, then replace the bracketed sections.

```
Write a Playwright end-to-end test in TypeScript.

Requirements:
- Use strict TypeScript
- Use Page Object Model (POM)
- Use ONLY data-testid selectors
- Follow AAA pattern (Arrange, Act, Assert)
- Use proper async/await (no hard-coded waits)
- Keep test clean and readable

Scenario:
Test login to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login
Username: Admin
Password: admin123


Output:
- login.spec.ts
- relevant page object files
```
