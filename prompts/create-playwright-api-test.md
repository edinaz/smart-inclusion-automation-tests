# Create Playwright API test (TypeScript, request API)

Copy everything inside the block below into your assistant chat, then replace the bracketed sections.

```
Write a Playwright API test in TypeScript.

Requirements:
- Use Playwright request API
- Follow AAA pattern
- Validate status codes and response body
- Use clean structure and reusable functions

Scenario:
[API endpoint + method]

Assertions:
[EXPECTED RESULTS]
```
