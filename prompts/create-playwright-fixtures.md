# Generate Playwright fixtures and test data setup

Copy everything inside the block below into your assistant chat. Add context (app, auth flow, env vars) as needed.

```
Generate Playwright fixtures and test data setup.

Requirements:
- TypeScript
- Reusable fixtures
- Clean separation of data and tests

Include:
- Test users
- Setup/teardown logic

Output:
- fixtures file
- example usage in test
```
