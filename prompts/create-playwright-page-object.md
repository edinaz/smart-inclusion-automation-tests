# Create Playwright Page Object (TypeScript, data-testid)

Copy everything inside the block below into your assistant chat, then replace the bracketed section.

```
Create a Playwright Page Object in TypeScript.

Requirements:
- Use class-based POM structure
- Encapsulate locators using data-testid only
- Provide reusable methods (actions + assertions)
- Keep code clean and readable

Page details:
[DESCRIBE PAGE + SELECTORS]

Output:
- Page class with methods
```
