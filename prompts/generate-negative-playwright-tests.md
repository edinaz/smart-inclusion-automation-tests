# Generate negative Playwright test cases (TS, POM, data-testid)

Copy everything inside the block below into your assistant chat. Attach the baseline test and existing page objects for context.

```
Generate negative test cases for this Playwright test.

Requirements:
- TypeScript
- POM pattern
- data-testid selectors only
- AAA pattern

Focus on:
- Invalid inputs
- Empty fields
- Incorrect credentials
- Edge cases

Return:
- Separate negative test cases
- Reuse existing page objects
```
